# postgres superuser pwd. It's not db_password in odoo.conf
POSTGRES_PASSWORD=<super user password>
POSTGRES_INITDB_ARGS=--encoding=UTF8 --locale=C
# db_user and db_password in odoo.conf
PGUSER=odoo
PGPASSWORD=<db_user password>
